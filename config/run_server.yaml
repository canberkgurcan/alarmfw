includes:
  - notifiers/zabbix.yaml
  - notifiers/smtp.yaml
  - policies/dedup.yaml
  - generated/ocp_cluster_snapshot.yaml

runtime:
  lock_file: "/state/alarmfw.lock"
  state_db: "/state/alarmfw.sqlite"
  log_level: "INFO"
  dry_run: false
  interval_sec: 120   # 2 dakikada bir çalış
